package com.example.workouttracker.Security;
/*
import java.security.SecureRandom;
import java.util.Base64;

public class KeyGenerator {
    public static void main(String[] args) {
        SecureRandom random = new SecureRandom();
        byte[] bytes = new byte[32]; // 32 bytes = 256 bits
        random.nextBytes(bytes);
        String secretKey = Base64.getEncoder().encodeToString(bytes);
        System.out.println("Tu Secret Key Segura: " + secretKey);
    }
}
// Ejecuta esto una vez, copia la clave, y luego borra el código.*/